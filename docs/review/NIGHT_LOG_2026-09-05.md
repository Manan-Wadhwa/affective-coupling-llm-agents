# Operator log — night of 2026-09-04/05 (auto mode)

Timestamps UTC. Every entry names the file or command it concerns. Written for morning review;
the results log (`RESULTS_LOG.md`) stays the record of *measurements*, this is the record of
*actions and observations*.

- 19:01 Both previous sandboxes confirmed dead (410). Two new boxes received: box3 `sb-58e32f35…`, box4 `sb-ea69c0d1…`. Both RTX PRO 6000, torch 2.11.0+cu130, transformers 5.14.1, empty HF cache.
- 19:05 `docs/planning/PREREG_B1c.md` written and committed (122af4c) before any B1c code — hypotheses H1/H1′, six arms, four doses, blocked dose-paired contrasts, median-blocked-fraction decision with an indeterminate zone.
- 19:07 B1c writer agent dispatched (one opus agent; spec = the pre-registration + `b1_followup.py`). Box3: 27B weights prefetched (done 19:12), accelerate installed with `--no-deps`, B1 probe pool uploaded (sha 70129f86 verified).
- 19:11 Box4 staged (drivers + 27B pool 56 chunks + 8B pool 78 chunks, all sha-verified) and `runner_box4.sh` launched: feature regeneration then three CPU sweeps (second anchor n=2000 on layers 43,54; layer 54 at anchor 600; 8B fixed-λ on layers 14,21,27).
- 19:18–19:20 Box4 regenerated both feature sets from the saved pools — kept counts 4253 and 3898, identical to the original runs.
- 19:25 B1c driver delivered with `--selftest` passing; `acl_core.paired_slope_contrast` gained `block=`/`pair_doses=` with defaults bit-identical (writer's 160 trials). Pre-launch addendum appended to the pre-registration and committed (cc2dc32): the median rule reads H1 on an even 3-vs-3 split; layer range 13–63; balanced panel; check semantics.
- 19:30 Verifier (one opus agent): LAUNCH-READY, no must-fix; six should-fix notes recorded in RESULTS_LOG as known limitations. Independent differential test 60/60 identical; `a3_scalefree` output byte-identical under the new core.
- 19:32 Driver + core + `--n-ref` + `stage_box.sh` committed (26e535d). Box3 staged (acl_core 3da064ad, driver 179d26a4, runner, prereg) and B1c launched. Pool resumed from cache: kept 1615 (= B1). Gate passed at all 51 layers (0.882–0.920 class mean; afraid weakest, 0.81–0.85 per class).
- 19:36 Monitor bug: counters keyed by file name collided across boxes (`runner.log`); fixed to key by box/file, monitor restarted.
- 20:31 Box3 156/432 arm-rows, 21 s each, GPU 99%. Box4 on its first sweep, layer 43; cells reproduce box1's bit for bit (tuned-C n=600: 0.5781/0.5998). Box4 fits ~2.5× slower than box1 — 8B sweep may hit its lease (box4 up since ~15:30Z).
- 20:40 User: auto mode, no input, log heavily. Plan for idle time: (1) committed re-analysis of B1 / B1-follow-up / A3 contrasts with the blocked, dose-paired bootstrap (`src/rev3/reblock_contrasts.py`); (2) refresh `results/README.md` for the rev-3 files added since the index was built; (3) dated addendum to `A0_PRIOR_ART.md` with the like-for-like cross-estimator number. Token use kept low: no further agents unless a result needs a critique.
- 20:38 Wrote and ran `src/rev3/reblock_contrasts.py` → `results/rev3/reblocked_contrasts.json`; A0_PRIOR_ART addendum; results/README rev3 section refreshed.
- 20:39 Reblocked re-read confirms critics: b1f emo−rand 2→3 neg, emo−ceiling pos 1→0, A3 dom→dom neg 0→1; registry entry reblock.b1f_emo_vs_rand_blocked added; RESULTS_LOG entry written. Committing.
