# Authors' revision notes — claimed fixes in the revised PDF, in response to Reviewers 1–3 (for the area chair)

Each item names the reviewer point it answers and where in the revised PDF the fix can be checked.

1. **Estimator-study pool described** (R1 W1-type, R2 mismatch 6, R3 W1, mentor 1): Section 2 "Two pools" names the 4,253-item (27B) and 3,898-item (8B) generation pools used for the split-half study, separate from the 1,615-item probe pool used for the ablations, and states why n = 2000 per half exists on one and not the other.
2. **Random-versus-none contrast computed** (R1 W3, R3 W3, R2 mismatch 8): Table 2 row "B1c random direction (rank 1)" and Section 5 B1c: inert for all six emotions, happy +13.5 [−0.1, +28.4], q ≥ 0.26 (`rev.b1c_random_vs_none`).
3. **Random 5-frame reported** (R2 W3, R1 W2): Table 2 row "B1d random 5-frame", inert (q ≥ 0.51); its footprint (3.1–3.2, 5× the emotion direction) in Table 3.
4. **Every arm against baseline in one table with one significance standard** (R1 W1, R3 W7, R2 W7ii): Table 2 (blocked fraction of the none slope, * = BH-FDR q < 0.05 within the arm's five testable emotions, · = unadjusted only); Table 1 re-marked with the same convention; Figure 4 redrawn from Table 2 with the marks explained.
5. **FDR status corrected** (R1 W1, stats audit 6–7): the permuted-label rank-5 frame's blocks clear BH-FDR against baseline (q 0.001 / 0.035 / 0.013); the q = 0.051 belongs to the affect-minus-permuted contrast and is placed there; against baseline sad's rank-1 block does not clear (q 0.115) and this is stated.
6. **Dose-0 readout shift for every B1d arm and the pc1 arm** (R1 W2, mentor): Table 3 column "dose-0 shift" (affect subspace +0.39/−0.25; permuted ≤ 0.08; random frame ≤ 0.01; pc1 +0.55/−0.28).
7. **Footprint defined once with one denominator** (mentor 4, R2 mismatch 3, R3 mismatch 3–4): Section 2 "Footprint" and Table 3; all ratios relative to the emotion direction in the same run; the "twice the norm / 1.22" pair reconciled (1.15–1.26 absolute = 2.0×); "0.85×" given with its per-emotion values.
8. **Other-speaker probe reported** (R2 W4, R3 W2): Section 4 "What the probe measures" gives the other-speaker projection's slopes beside the present ones (`rev.other_probe_rises`) and withdraws the "its own state" construct claim in the abstract and contributions.
9. **Dose-response shape** (R1 W5, R2 W7iv, R3 W4, mentor 2): Figure 3 now carries scenario-bootstrap bands on every arm and paired dose-step changes in each title; Section 4 reports happy's saturation and calm's reversal (`rev.dose_shape`); the caption no longer asserts separation for calm.
10. **"Bit for bit" reclassified as a determinism check** (R1 W10, R3 W6): Section 1 and Section 4; the independent sampling replication is the B1 follow-up run.
11. **Per-layer refitting and "readable layer" defined** (R2 W5, Q1–2): opening paragraph of Section 5.
12. **Held-out accuracy and projection correlation** (R2 W2, mentor Q9): Section 3 "Is this just n < d, and does it cost anything?": logistic decodes held-out items better (0.93 vs 0.88 at n = 1200) and the two estimators' projections correlate at 0.91–0.96.
13. **Low-dimensional control** (R2 W1, Q6): same paragraph; projected to d′ = 100 the fits stop separating (training accuracy 0.97 at n = 2000) and logistic reproducibility rises to 0.90 against difference-of-means 0.94 in standardised coordinates (`a2l.lowd_recovers`; 8B 0.81 vs 0.85, `a2l.8b_lowd`).
14. **Regime-conditional wording** (R1 W9, R2 W1): abstract and Section 3 state the n < d condition explicitly.
15. **Median with calm and per-emotion values** (R1 W11, R3 W3, stats audit 17): Section 5 B1c gives the five per-emotion values, the median with calm (15%), and labels the interval a scenario bootstrap of the median; the testability rule's pre-registration is cited in Appendix A.
16. **Numerical mismatches corrected** (all reviewers): tuned-C 8B "0.38 (0.37–0.41 across three depths)"; "hundredfold or thousandfold" with C = 50 and 500 named; related-work range "0.55–0.61 / 0.28–0.38"; 0.63 vs 0.43 explained as two different mean-difference conventions; "27-fold" restricted to the 27B; the rank-5-vs-rank-1 numbers given in slope units with their sign convention; Figure 1's 8B axis ends at 1200; the fixed-penalty anchor is named in every figure and caption.
17. **Title** now says three pre-registered ablations (R1 W7).
18. **Captions rewritten from the panels** (mentor 6, R1 W7, R3 W4): Figures 1, 3 and 4.
19. **Citations** (citation audit): Braun et al. characterised correctly (they do refit; mean-difference vectors converge at 200–500 draws); RAPTOR's robustness quoted as 0.87–0.98; Soudry et al. (implicit bias on separable data), Benjamini–Hochberg, Efron–Tibshirani, and two emotional-contagion papers added; Panickssery/Turner records updated.
20. **Abstract narrowed** (R3 W5, stats audit 3/5/10): "a fitted, label-free control blocks where the random one is inert" now specifies the rank-5 permuted frame, states that the rank-1 permuted direction and the random 5-frame are inert, and says the rank/footprint question is untested; "at most a third" removed; the 8B afraid caveat added.
21. **Title** changed to "Steered affect moves a conversational partner's activations: …" so it no longer asserts the transfer construct that Section 4 withdraws (Reviewer 2's recalibration condition).
22. **Anonymity** (R2): the submitted PDF still carries the author line; the camera-ready decision is the authors', and the AC is asked to disregard it.

Not done in this revision (stated as limitations): an ablation rerun with the logistic direction; across-draw variability of the direction estimate; a footprint-scaled random 5-frame; cross-experiment multiplicity control; n > d on the full width.
23. **Footprint-matched random frame run** (AC condition 4; R1/R2/R3): PREREG_B1d addendum 2 (committed before launch, sha c3bbe034 stamped in the result file). A rank-23 Gaussian random frame whose removed norm matches the permuted-label frame's (6.7–6.9 vs 6.5–6.9 per position) is inert for all five testable emotions (blocked fractions −0.03 to +0.07, BH-FDR q ≥ 0.71; `b1d23.inert`, `b1d23.footprint_matched`). Added as a row to Tables 2 and 3; §5's "points at footprint rather than rank" sentence replaced; §6's open question closed ("matching the control's footprint is not enough; the control has to be fitted by the intervention's own estimator"); abstract updated; Limitations notes calm's out-of-family shift (−30 [−55, −4]). The run's none arm agrees bit for bit with B1d's, so the contrast sits on the same baseline.
24. **Direction-resampling draws run** (AC condition 3; R1): PREREG_B1e addendum 3 (sha 4cb36929 stamped in all ten files). Ten re-splits of the probe pool (seeds 1–10), re-estimating steering direction, ablation directions, permuted control and readout probe; afraid and sad; arms none / emotion / permuted. Sad's block against the permuted control holds in 10/10 draws (median 35%, 24–56%); afraid's is 18% (7–23%), significant in 9/10, but afraid's manipulation check passes in only 4/10, so the affect-specific claim is narrowed to sad in the abstract, introduction, §5 and conclusion. New appendix table (`table_b1e_draws.tex`), Limitations and Appendix B updated; `b1e10.sad_stable`, `b1e10.afraid_gate_fragile` registered. The earlier "afraid (25%) and sad (29%) survive" sentence is gone; 25% was the top of afraid's spread.
25. **Round-2 review fixes** (R1, R2, R3, citation validator, claims-and-numbers checker; all on the version with items 23–24): (a) "as strongly as the affect subspace" replaced everywhere by "at the same false-discovery threshold, at half to two-thirds of its size" (Table 2: 0.27/0.23 vs 0.54/0.40); (b) "a third of sad's response in every draw" → "at least a quarter" / "a quarter to a half" (Table 4 range 0.24–0.56); (c) draw 0's afraid value described as above the ten-draw range, not "at the top of" it; (d) the rank-23 frame called inert "on the five testable emotions" with calm's shift stated (abstract, §5, §6); (e) "bites because it is fitted" softened to "built from the pool's activations", with the covariance-matched random frame named as the untested discriminating control (R2); (f) permuted frame "no emotion information" → "no label information (removes 7–52% of readable affect)"; (g) "the mechanism is the n<d regime itself" → "necessary, not the whole of it"; (h) "replications of two designs on the 8B" → "two designs rerun, only sad carries over"; (i) "invisible in transcripts" → "need not show in transcripts, which we do not examine"; (j) abstract's 83%/85% parenthetical attached to the right comparisons; (k) other-speaker "as much as" → "comparably"; (l) "most work uses" logistic → "probe studies use"; (m) Limitations: denominator uncertainty of blocked fractions and single-realisation controls stated; (n) prior work: Cover 1965, Rosset–Zhu–Hastie 2004, Arditi et al. 2024, Hewitt & Liang 2019, Elazar et al. 2021, LEACE, two emotion-steering preprints, a multi-agent survey added; Durupinar recharacterised; Herbster's 0.80 case and shared model stated; Braun "draws" → "training samples"; (o) checker's mismatches: Table 3 B1c dose-0 shift ≤0.08, pc1 readable-affect range 0.01–1.5, footprint ratios put on one mean/mean convention (11.6/11.2/5.4/11.4), Table 2 calm +0.68, "two of twenty" emotion-draws, 8B under an hour, 81 iterations, 0.0056, 0.92→0.43, q<0.001, arm-row counts, 220-per-cell 8B pool, 240-token estimator pool, anchor-600 dip 0/10, "top principal direction blocks angry, one of those three", dose-step wording, permuted direction "at most 9% of readable affect"; (p) Figure 4 moved to the appendix to keep the main text on nine pages. Not addressed (need runs): permutation distribution of the permuted-label control; covariance-matched random frame; behavioural readout on B; re-draws of B1c/B1d; the 0.004 cosine remains unstamped.
26. **Round-2 area chair conditions** (metareview in `reviews/round2/metareview.md`: main track reject 4/10; IAB poster 6/10, spotlight if conditions 2, 3 and 12 are met). Met from committed data without new GPU runs: 1 (wording matches tables, see note 25), 4 (Appendix C, Table 5: every arm − none in slope units with CIs), 5 (Table 4 caption states the unadjusted rule and the absent family), 6 (six-emotion-family q-values in Appendix B: sad 0.030, calm 0.011; `results/rev3/b1c_fdr_six_family.json`), 7 (all numerical corrections), 8 (8B named as Llama-3-8B-Instruct-abliterated in the abstract and §2 with its qualifications; the reason for the abliterated checkpoint is not recorded in the repository, so none is stated), 9 (Appendix C, Table 6: words, degenerate and refusal rates, distinct-2 and perplexity per arm; blocking arms within one word and 0.1 perplexity of baseline; the top principal direction is the exception), 10 (rank, footprint and metric named; rank and metric stated as confounded), 11 (Candès & Sur 2020 and Ravfogel et al. 2020 added to the nine references of note 25). Not met, need runs: 2 (covariance-matched random frame), 3 (permutation distribution of the rank-5 control; labelled a single realisation in Limitations), 12 (transcript-level readout). Blocked-fraction intervals still divide by the none-arm point estimate (condition 4, second half); stated in Limitations.
27. **Permutation null run** (AC condition 3; R1): PREREG_B1d addendum 3 (written before launch). Four fresh label permutations of the rank-5 permuted-label frame (`--perm-seed` 1–4, permuted arm only, contrasted against B1d's stored none arm) block afraid, sad and angry together in 1/4; five-realisation medians afraid 0.02, sad 0.34, angry 0.20; contrast below zero in afraid 2/5, sad 4/5, angry 5/5. Fixed reading: unlucky draw (three-emotion pattern downgraded to a single-realisation observation; angry 5/5, sad 4/5, afraid 2/5 stated). New appendix table (`table_perm_null.tex`, Table 7); abstract, introduction, §5, §6, Limitations, Appendix A and B updated; `b1dperm.reading`, `b1dperm.counts` registered. Condition 3 of the round-2 metareview is now met; 2 (covariance-matched random frame) and 12 (transcript-level readout) remain unrun.
28. **Final independent review** (`reviews/round2/final_review.md`, all four angles, on the bundle with the permutation null; NeurIPS 4/10, IAB 6/10 poster). Applied: abstract's permuted-frame sentence now says the affect subspace clears q<0.05 on two emotions and the permuted frame out-blocks it on afraid; "(unadjusted intervals)" on the permutation counts; other-speaker projection "also rises with dose" (units differ); the covariance concession added to the abstract; intro's "rather than sample size or penalty" replaced by "no sample size within reach (n ≤ 2000 against d = 5120) and no penalty schedule removes"; intro's 24% attributed to the permuted control (23% against baseline); footnote states the two review rounds were internal, by language-model reviewers, and that the paper has not been peer-reviewed; §3 penalty sentence marks both fits tolerance-limited; §4 B1 follow-up sentence rewritten; Figure 3 caption adds angry in the 13–42 window; Appendix B gives the driver's 0.048 beside the recomputed 0.037; Conclusion's "on both models" replaced by "only sad's block carries over to the 8B". Declined: title change (reviewer's taste); the reason for the abliterated 8B (not recorded). Not addressable without runs: a random frame inside the pool's top principal subspace (condition 2); a permutation null large enough to test the affect subspace against. To keep nine pages, four sentences stated elsewhere were cut (§5 secondary readout, §6 "study with only the random control", Limitations dose-shape and A-span-readout sentences, the appendix pointer) and Figures 1–3 shrunk a notch.
29. **Emotion ordering of the permutation null** (PI, after the final review): Section 6 now states that the permuted frames' per-emotion counts (angry 5/5, sad 4/5, afraid 2/5) match the affect subspace's own ordering in Table 2 (angry and sad at q<0.05, afraid not), read as: which emotion a rank-5 frame from this pool blocks is set by the emotion's place in the pool's geometry more than by the labels; labelled exploratory on five permutations. The abstract names angry and sad as "the two the affect subspace also blocks". Paid for on page 9 by cutting clauses stated elsewhere (rank-5-vs-rank-1 slope differences, now Table 5 only; the 23-frame footprint clause and the "arms that block are estimated from the pool" line in §5, kept in §6; the frame-geometry cosine; the n<d sentence in Limitations, kept in §3; the Conclusion's opening line; "afraid's is less reliable" and the logistic-rerun item, both still in §5/§7 elsewhere) and by moving the page-9 enlargement to the Limitations header.
